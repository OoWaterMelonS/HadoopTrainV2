统计页面的浏览量
    count
    一行记录做成一个固定的key，value赋值为1（这个地方个人理解为权重）
统计各个省份的浏览量
    select province count(1) from xxx group by province;
    地市信息我们是可以通过ip解析得到的  《== ip如何转换为城市信息
    ip解析： 收费   此处使用开源的一个解析工具+解析类
统计页面的访问量:
      把符合规则的pageId获取到，然后进行统计即可。
      个人总结：这个地方使用了java的语法基础  Pattern和Matcher的组合使用.这两个类的使用还需要看一些代码

==》 每一个mr作业都去全量读取待处理的原始日志，如果数量很大，性能完全不足

ETL :全量数据，不方便直接进行计算，最好进行异步处理后再进行相应的维度统计分析
    解析出需要的字段 ：ip  =>城市信息
    去除一些不需要的字段：不需要的字段就太多了..
    ip/time/url/page_id/country/province/city
大数据处理完以后的数据现在存放在HDFS上
大数据所做的工作基本这么多
再进一步：使用技术或者框架把处理完的结果导出到数据库中
        sqoop:把HDFS上的统计结果导出到mysql中
